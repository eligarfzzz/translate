# 03: 文档同步——规则措辞、ADR 演进段、用例计数

**What to build:** 把这次改动的语义写进仓库的领域文档与决策记录：`CONTEXT.md` 的「空即未设置」规则里那句"空串默认值字段（三个 API 字段）的描述性假示例"按新规则改写——示例是**可选**的字段说明，不再是空默认字段的必配项，占位规则表述为"默认值非空的字段占位即默认值本身"；`ADR-0003` 追加一段 `Evolved:`（append-only，不动旧段）记录新增 `extra` 键、`{extra}` 成为默认模板的一部分、示例不再强制；`AGENTS.md` 的用例计数按实测更新。

**Blocked by:** 01, 02（文档要照最终实现与最终用例数写）

**Status:** resolved

- [x] `CONTEXT.md` 的「空即未设置」规则：占位文字那段改为"默认值非空的字段占位即默认值本身；空串默认值字段**可以**配一个描述性示例（三个零痕迹 API 字段就是这么做的）"，不再表述为必配；其余句子（空字段删键、全空删 `config` 键、读取从默认全量叠加非空值、恢复默认 = 清空 + 保存 = 删键、判空按默认值类型且只由配置模块的归一化入口决定）**逐字保留**。
- [x] `ADR-0003` 追加一段 `Evolved:`（放在最后，**不改任何既有段落**）：新增 `extra`（追加提示词）键；默认模板新增 `{extra}` 占位符，追加内容插到指定位置；追加提示词里写的占位符字面保留（`renderPrompt` 改为只扫描模板一次）；空串默认值的字段不再被强制要求配占位示例，占位规则收敛为"非空默认值 → 占位即默认值"。
- [x] 若本次改动涉及保存反馈口径，在同一段里记一句：反馈只报"存储里原本存在、本次被清空的键"，不再报表单里所有空项。
- [x] `AGENTS.md` 里的用例数按 `npm test` 实测值更新（改动前后各测一次，写实测数字，不写推算值）。
- [x] 文档中不得出现真实端点、密钥或厂商模型名（零痕迹）；示例沿用仓库既有的 `example.com` / `sk-example-not-a-real-key` / `example-model` 写法。
- [x] `npm test` 全绿（lint 含 prettier 检查，Markdown 也在格式检查范围内时不得破格式）。

## Comments

- 本票只动文档，不动源码与测试；ADR 的 `Evolved:` 段落是追加，历史段落一律不改写。
- 「追加提示词」是否需要进 `CONTEXT.md` 的 Language 词表：**不进**——它是配置页的一个字段，不是像「宿主」「并发池」那样需要统一语言的领域概念；它的语义由「空即未设置」这条既有规则覆盖。
- 实施于 commit `95c86e2`；reviewer-verify 两轴审阅 PASS（0 blocker），范围 `95c86e2~1..95c86e2`（3 文件，+6/−2）。6 项验收全部 PASS。
- 评审的独立验证：`CONTEXT.md` 仅变动 1 行（numstat 1/1），被替换句段两侧的公共前缀 365 字符、公共后缀 419 字符逐字相同，五句（空字段删键 / 全空删 `config` 键 / 读取从默认全量叠加非空值 / 恢复默认 = 清空 + 保存 = 删键 / 判空只由配置模块归一化入口决定）全部落在字节相同的首尾内；`ADR-0003` 为 4 增 0 删，父文件内容是子文件新内容的精确前缀（append-only 成立）；`AGENTS.md` 记 177，在被审提交上实跑 `npm test` 得 `tests 177 / pass 177 / fail 0`。
- 零痕迹：本票未新增任何示例文本；新增行正则扇描无真实端点/密钥/厂商模型名。
- 非阻断措辞 nit（评审记录，未修）：ADR 新段写 "silently — no placeholder is inserted into their template and no notice is shown"，若读作"用户永远不会被告知"，与 `options.html` 里常驻提示「模板里没写 `{extra}` 时它不生效」相抵触；读作"保存期无专门提示"则与实现一致（只有缺 `{host}` 的模板才有专门保存文案）。可选收紧为 "no save-time notice"。
- 非阻断历史事实（与 skip-home 前的描述不符，评审记录）：历史计数 165 是在 `de86d35` 版本下用同一命令（`node --test`）测得 165 得以验证，与 `AGENTS.md` 当时的写法一致。
