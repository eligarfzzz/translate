# 03: 文档同步与全量验收

**What to build:** 让"空即未设置"成为项目语言的一部分，后来者读文档就知道：空值不持久化、读取回退默认、「恢复默认」= 清空 + 保存 = 删键（因此永远跟随版本默认，不会被冻结在旧默认上）。同时确认全仓没有旧双语义的命名与说法残留。

**Blocked by:** 02（占位与「恢复默认」的最终行为）

**Status:** resolved

- [x] 领域文档的 Rules 增加一条"空即未设置"：空值不落盘（整包重写，空键即删除）、读取先取默认再被非空值覆盖、配置页空框以默认值占位、「恢复默认」= 清空 + 保存 = 删键；判空按默认值类型界定且只有一处实现（配置模块内的归一化入口）。
- [x] ADR-0003 追加第三段演进记录：raw/runtime 双语义收敛为一条、上一段警告的"恢复默认即冻结"有了 UI 级解冻路径、纯空白与类型错误不再被当作有效值；并说明原决策（零痕迹默认 + 同步存储覆盖 + 单一 seam）未变，本次只是语义收窄。
- [x] 历史段落只追加不改写：既有 "Fulfilled" 与两段 "Evolved" 保持原文，新段标注为演进。
- [x] 文档与工单里的示例一律为明显假值（`example.com` / `sk-example…` / `example-model` 一类），全仓不出现真实端点、凭证或厂商模型名。
- [x] 全仓检查：配置页提示文案、注释、文档中不再有"保存什么读什么 / 空值透传 / 双语义"的旧说法，也不再有已删除加载器的命名残留。
- [x] `npm test` 全绿（lint + node:test），并确认清单一致性检查与静态守卫

## Comments

- 实施于 commit `5cc2143`；reviewer-verify 两轴审查 PASS（NO_BLOCKER）。Reviewer 以执行证据确认 ADR 追加段落为 4 行插入 / 0 行删除（历史段落字节不变）、`src/` 仍为 16 个文件、`npm test` 154/154、静态守卫 8/8。
- 票面勘误（非阻断，评审指出）：本票措辞“ADR-0003 追加第三段演进记录”与实际不符——落笔时 ADR-0003 已有三段 Evolved（零痕迹默认 / promptTemplate 成为普通默认键 / 配置页 TAB 分组），本次新增的是**第四段** Evolved（全文标题为 “Evolved: empty means unset — …”）；验收目标（追加演进、原决策未变）已达成，仅序号口径需知悉。（配置页入口、模块图、注入物选择器单一来源）未因本次重构失效。
