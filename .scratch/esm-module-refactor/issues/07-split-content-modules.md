# 07: 内容脚本按职责拆分——改动画不必读端口协议

**What to build:** 内容脚本从一个三百多行的单文件拆成四块各自单一职责的模块：会话状态与生命周期（会话标志、代号作废、在途端口登记、还原）、译文节点写入与加载动画、单宿主端口流程与并发池、页面观察器与防抖调度。翻译行为完全不变。

**拆分不新增测试接缝**：四个新模块不各自加测试，全部继续透过「消息与端口进 → 译文节点出」这一条接缝观测。否则拆分会长出四个新接缝，把内部结构焊死在测试里，下次想换结构又得先改测试。

**Blocked by:** 05（拆分靠模块间 import 表达依赖；在全局体系下拆只是把问题挪个地方）

**Status:** done（已实施：33a0053，lint 0、101/101 绿）

- [x] 四块职责各自成模块，依赖关系由 import 表达，无循环依赖
- [x] 会话安全边界不回归：会话外不发请求、不写 DOM；还原后在途请求全部中止，还原后写入绝不上屏
- [x] 不新增测试文件、不新增测试接缝；内容脚本集成测试断言一字未改
- [x] 98 个既有测试全绿，lint 零告警（实际 101：98 − 3 + 5 由票 05 账目解释，+1 修复票回归用例）

## Comments

- 2026-08-29：实施于 commit 33a0053。拆为 content-session / content-render / content-translate / content-observer 四块 + content.js 装配 + content-loader.js 入口。review（reviewer-verify，含运行时探针）无 blocker：会话安全边界逐条确认未回归；6 项 Standards 判断题不阻塞，1 项旧有缺陷（非本次回归）记录在案。
