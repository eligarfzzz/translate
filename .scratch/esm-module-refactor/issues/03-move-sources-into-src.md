# 03: 源文件收进 src/——纯搬家，行为与加载方式都不变

**What to build:** 全部源文件收进一个 `src/` 目录（原 `lib/` 并入），根目录只剩清单、配置页 HTML 与工具配置文件。扩展照旧工作，测试照旧全绿。

这是为换轨做的铺垫（先让改动变容易，再做那个容易的改动）：可访问资源需要一个能一句话覆盖的路径前缀，根目录散着源文件做不到——要么逐个列举，要么用通配把清单之外的东西也扫进去。搬家单独成票，是为了让换轨票的 diff 里不混入几百行「文件换了位置」的噪音。

**Blocked by:** 02（先格式化再搬家，搬家 diff 才是纯粹的重命名）

**Status:** done（已实施：079bf1d，lint 0、98/98 绿）

- [x] 源文件全部移入 `src/`，目录内不再按角色细分（对当前体量是过度分类）
- [x] 三处引用清单同步：扩展清单的内容脚本数组与 service worker 声明、配置页的脚本标签、service worker 的脚本导入调用
- [x] 测试与测试基建中的路径引用同步，加载顺序语义不变
- [x] lint/format 覆盖新目录
- [x] 除路径外无任何内容改动（导出包装、全局赋值一律不动，留给换轨票）
- [x] 98 个既有测试断言一字未改，全绿

## Comments

- 2026-08-28：实施于 commit 079bf1d（19 文件，全部识别为重命名，+49/−47 限于路径引用与 02 的格式化换行）。review（reviewer-verify 前身，只读）无 blocker，确认无 `lib/` 幽灵引用残留。
