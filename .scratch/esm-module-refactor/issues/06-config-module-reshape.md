# 06: 配置模块重塑——两条语义一份实现，模板降为普通默认项

**What to build:** 配置的两条读取语义（运行时空值回退默认 / UI 回填保存什么读什么）共用一份实现，差异收敛成一个参数——改判空规则时不可能只改一半。提示词模板成为默认值表里的普通一项，不再由另一个模块挂出来供配置模块消费。配置的读写行为对用户完全不变：清空输入框保存后 UI 显示空、运行时用默认模板、端点/密钥/模型缺一项仍给出「打开配置页」指引。

**Blocked by:** 05（storage 注入依赖模块化后的函数签名；模板降级依赖跨模块 import 而非全局）

**Status:** done（已实施：ad0f705，lint 0、101/101 绿）

- [x] 两条语义合成一份核心实现 + 两个薄入口，判空差异只存在于一处
- [x] 提示词模板进入默认值表，两个语义分支里的模板特判块删除——通用字符串分支产出的结果与今天逐字一致（运行时空值回退默认模板、UI 空串透传）
- [x] 模板模块不再挂任何全局；service worker 里那句加载顺序注释随之消失
- [x] 配置读取接受注入的 storage；测试传替身而非依赖「没有扩展 API 所以走兜底分支」
- [x] 消费侧「模板不含 {host} 时回退默认」的兜底保留不动
- [x] 配置相关测试断言一字未改，全绿；lint 零告警

## Comments

- 2026-08-29：实施于 commit ad0f705。review 无 blocker；两处 minor 记档：UI 路径「逐字一致」仅限 spec 点名的两条行为，缺键/非字符串场景有意变化（commit message 已记）；已保存过旧模板的用户被冻结在旧默认值上，已记入 ADR-0003 演进段落。注：storage 注入在票 05 已提前兑现（删环境探测所需），本票核心为语义合一与模板降级，无重复劳动。
