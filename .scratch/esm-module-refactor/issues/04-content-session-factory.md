# 04: 内容脚本会话工厂化——环境依赖改为注入，行为不变

**What to build:** 内容脚本不再是一个读全局的立即执行闭包，而是一个接受环境依赖（文档、扩展 API、计算样式）的会话工厂，由文件末尾用真实全局启动——形态对齐宿主发现模块已经在用的那个更好写法。翻译、还原、动态补翻、错误提示、并发上限的行为完全不变。

这是换轨票里最容易出错的一块，单独拆出来先做：它是纯粹的结构改动，能在现有加载体系下独立验证；等换轨时那一步就只剩「删掉末尾的启动调用，改成加载器来调」。测试基建同步改为直接调用工厂并注入 jsdom，观测面（消息与端口进 → 译文节点与在途端口峰值出）一字不变。

**Blocked by:** 03（路径先稳定，避免同一批文件被两票连续大改）

**Status:** done（已实施：79c87fa，lint 0、98/98 绿）

- [x] 内容脚本导出会话工厂，环境依赖显式注入；文件末尾用真实全局启动，扩展行为不变
- [x] 内部不再直接触碰注入进来的那些全局
- [x] 测试基建改为调用工厂并注入 jsdom 环境；手动时钟、mock 端口、消息总线保留
- [x] 内容脚本集成测试的断言一字未改
- [x] 拆分本身不新增测试接缝（拆分留给后续票，观测面始终是这一条）
- [x] 98 个既有测试全绿，lint 零告警

## Comments

- 2026-08-28：实施于 commit 79c87fa（2 文件：src/content.js 与测试基建）。review 无 blocker；两项建议已并入后续票：`document.defaultView === window` 真机验收并入 05 清单，`globalThis.chrome` 桥接删除随 06 的 storage 注入兑现。
