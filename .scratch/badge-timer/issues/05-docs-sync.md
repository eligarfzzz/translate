# 05: 文档同步——徽标两段式读数

**What to build:** 票 04 改了对外可见行为（时间读数的出现时机），把新语义写进领域文档：`CONTEXT.md` 的「进度徽标」条目要把"只在全部落定后才显示"改成两段式；`AGENTS.md` 里 `tests/` 的用例数按实测更新。

**Blocked by:** 04（必须在 04 的提交之后动笔——用例数取决于 04 加了几条断言）

**Status:** ready-for-agent

- [ ] `CONTEXT.md` 的「进度徽标」段落：改写时间读数那句——正文（档 0/1）全部落定时先给 `<main>s`；全部宿主（含 边缘区域）落定时补成 `<main>(<all>)s`，括号外仍是正文定格值；只新增 边缘区域 宿主时括号撤下、`<main>` 保持；新增正文宿主则两者都解冻、读数整体撤下；零宿主页面永不出现读数；无主内容宿主的页面只在全部落定后给 `0(<all>)s`。
- [ ] 既有句子**只许改与时序口径直接冲突的那部分**，其余（百分比、分子分母、错误括号、还原、注入元素、长段落风格）逐字不动；本段历来是单行长段落，保持该写法。
- [ ] `AGENTS.md` 的 `tests/` 用例数改为票 04 提交后的**实测值**（跑 `npm test` 数出来，不许推算、不许估）。
- [ ] 文档中不得出现真实端点、密钥、模型名。
- [ ] 本票只动 `CONTEXT.md` 与 `AGENTS.md`；不碰源码与测试；不新增 ADR（spec D9）。
- [ ] `npm test` 全绿（lint 含 prettier 检查，会校验根 Markdown 的排版；`.scratch/` 与 `docs/adr/` 是例外）。
- [ ] 证据：`git diff` 显示旧句子是**替换**而非整段重写（未涉及的句子逐字节不变）；用例数与 `npm test` 输出一致。

## Comments

- 口径来源：spec 的 §1（两段式显示时机）、§4（按集合分别解冻/回退）、D1 / D2 / D11。
- 该段落上一版是票 02 写的（"The reading exists only while everything has settled"），语义已被票 04 推翻，必须改；票 02 已 resolved，其验收记录不动。
- 改写时保留原有的英文行文风格与既有词汇（`Translate`、`还原`、`边缘区域`、`宿主`、`rescan`），不要顺手把长段落拆成多段或改标题。
