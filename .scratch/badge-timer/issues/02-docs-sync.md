# 02: 文档同步——`CONTEXT.md` 徽标口径与用例计数

**What to build:** 把徽标新增的耗时读数写进领域文档。`CONTEXT.md` 的「进度徽标」段落补上格式与语义：追加段 `<主内容>(<全内容>)s` 的含义（主内容档 0/1 落定时刻、含边缘区域的全量落定时刻、单位秒）、显示条件（只在全部已发现宿主落定后出现）、隐藏条件（重扫发现新宿主即撤下，再次全部落定后带着继续累计的读数回来）、无主内容宿主时括号外为 `0`、还原清除。`AGENTS.md` 的用例计数按 `npm test` 实测值更新。

**Blocked by:** 01

**Status:** resolved

- [x] `CONTEXT.md` 的「进度徽标」段落：保留既有全部句子（百分比口径、`<totalAll>` 口径、错误括号口径、百分比向下取整、上屏与还原、注入物永不成为宿主），只**新增**耗时读数的表述——格式 `<pct>% <done>/<total>(<totalAll>)(<errors>) <main>(<all>)s`；两个数字分别是主内容（档 0/1）与全部宿主（含边缘区域）**全部落定那一刻**的已用秒数（向下取整、一次定格）；仅当所有已发现宿主落定后才出现；重扫发现新宿主时撤下、再次全部落定后重新出现（主内容时间只在新增主内容宿主时解冻）；任何已发现宿主都没有的页面不显示时间；无主内容宿主时 `<main>` 为 `0`；还原清除。
- [x] `AGENTS.md` 的 `tests/` 用例数按 `npm test` 实测值更新（改动前后各测一次，写实测数字，不写推算值）。
- [x] 文档中不得出现真实端点、密钥或厂商模型名（零痕迹）。
- [x] 本票只动文档，不动源码与测试；不新增 ADR（展示口径扩展，不改架构接缝）。
- [x] `npm test` 全绿（lint 含 prettier 检查，Markdown 也在格式检查范围内时不得破格式）。

## Comments

- 本票的语义以票 01 的最终实现为准；`CONTEXT.md` 的段落是英文行文（本仓惯例），中文领域术语（正文区域/边缘区域/宿主/译文节点/还原）保持既有写法。
- 措辞注意：读数是**全部落定后的定格值**，不是"实时已用时间"——不要写成 live elapsed time 之类的表述，否则与"新段落出现即隐藏"的行为自相矛盾。
- 实施于 commit `44e59cb`；reviewer-verify 两轴审阅 **PASS / 0 blocker**，范围 `28befb3..44e59cb`（2 文件，+2/−2）。5 项验收全部满足。
- 评审的独立验证：既有两个句子块与父提交**逐字节相同**，只插入一段（979 字符）；八项必需语义点逐条作为字面子串命中；`npm test` 实测 **184/184**、`npm run lint` exit 0（prettier 覆盖根 Markdown，`.scratch/` 与 `docs/adr/` 是仅有的例外）；零痕迹、未新增 ADR、`src/` 仍 16 文件。
- 评审用 jsdom 驱动已提交模块复现文档所述行为（含 `100% 2/2(3)` → `100% 2/2(3) 12(60)s` 的 `12(60)s` 场景），文档与实现一致。
- 非阻断记录（未修）：该段落现为单行 1577 字符（原 598 字符），仓库已有同类长段落先例；"both floor … taken once" 单独读略欠精确（紧随其后的句子已明确解冻规则）；「全部落定」条件在段内出现两次。
- **口径已被票 04 修订（两段式读数）**：显示时机不再是"只在全部已发现宿主落定后出现"——正文落定先给 ` <主>s`、全内容落定再补括号。本票 `**What to build:**` 与第 1 条验收项里的"仅当所有已发现宿主落定后才出现"即此处被修订的表述，`CONTEXT.md` 的实际文案由票 05 改写。
- 本票 Comments 里"读数是全部落定后的定格值，不是实时已用时间"的**结论仍成立**（仍是定格值、不是秒表），但它据以推理的前提"新段落出现即隐藏"在新口径下变成了两段式的撤下/回退（见 spec §1）。
- 本票「评审用 jsdom 驱动…（含 `100% 2/2(3)` → `100% 2/2(3) 12(60)s`）」这条证据，在新口径下该序列多一个中间态：`100% 2/2(3)` → `100% 2/2(3) 12s` → `100% 2/2(3) 12(60)s`。当时记录不追改。
