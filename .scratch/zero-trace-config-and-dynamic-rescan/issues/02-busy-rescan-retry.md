# 02: 重扫忙则重试——翻译进行中出现的新内容不再漏翻

**What to build:** 初始翻译仍在流式输出的过程中，页面动态新增了英文内容（展开动态节点、插入节点），等当前翻译结束后这些新内容也会被补翻，不再被静默丢弃。重试调度在页面静止且翻译全部结束后自然收敛，不产生持续空扫；「还原」后监听与调度清理干净，会话外的页面变化绝不触发任何翻译。

**Blocked by:** None (can start immediately；与票 01 互不依赖，可并行)

**Status:** ready-for-human（已实施 3591406：两轴评审通过、无 blocker、86/86 测试绿；待人工验收）

- [x] 建立可复用测试基建：jsdom 沙箱按 manifest 顺序加载全部 content script、可控时序的 mock 批次端口（可停在流式中途再放行完成）、元素几何补丁（jsdom 几何恒为零的问题）、消息监听捕获
- [x] 回归用例：翻译流式进行中注入新宿主节点 → 断言最终出现译文容器（修复前红）
- [x] 防抖到期遇翻译进行中改为重新排队重试，不再静默丢弃
- [x] 页面静止后调度收敛：无持续空扫、无请求风暴
- [x] 会话外页面变化不触发翻译（既有安全边界不回归）
- [x] 既有全部测试保持绿色
